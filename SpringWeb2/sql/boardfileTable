CREATE TABLE BOARDFILE(
	NUM NUMBER(22,0) NOT NULL PRIMARY KEY,
	BOARDNUM NUMBER(22,0) NOT NULL,
	FILENAME VARCHAR2(100)
);

CREATE SEQUENCE BOARDFILE_SEQ;

ALTER TABLE BOARDFILE 
ADD CONSTRAINT FK_BOARDFILE FOREIGN KEY (BOARDNUM)
   REFERENCES BOARD (NUM);


/*
CREATE UNIQUE INDEX SYS_C004056 ON BOARD(NUM ASC);

ALTER TABLE BOARD 
ADD CONSTRAINT SYS_C004057 FOREIGN KEY(BOARDNUM)
	REFERENCES BOARD(NUM)
	ON DELETE RESTRICT
	ON UPDATE CASCADE;
*/